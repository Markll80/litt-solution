import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    map: {
      flex: 1,
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: 0,
      right: 0,
      zIndex: -1
    },
});

export default styles;